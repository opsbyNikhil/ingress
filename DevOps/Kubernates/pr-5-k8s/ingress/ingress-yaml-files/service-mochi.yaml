apiVersion: v1
kind: Service
metadata:
  name: mochi-svc-deployment
  namespace: prod
spec:
  type: ClusterIP
  selector:
    app: mochi-app
  ports:
      port: 80
      protocol: TCP
      targetPort: 80